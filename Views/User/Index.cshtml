@model IEnumerable<ManagementSystem.Models.tb_user>

@{
    ViewBag.Title = "Index";
}


<body>
    <!-- Begin Page Content -->
    <div class="container-fluid">

        <!-- Page Heading -->
        <h1 class="h3 mb-2 text-gray-800">Kemaskini Pengguna</h1>


        <!-- DataTales Example -->
        <div class="card shadow mb-4">
            <div class="card-header py-3">

                <h6 class="m-0 font-weight-bold text-primary">Senarai Pengguna</h6>
            </div>

            <div class="card-body">
                <div class="table-responsive">
                    @if (TempData["AlertMessage"] != null)
                    {
                        <div class="alert alert-success">
                            @TempData["AlertMessage"]
                        </div>

                    }
                    @Html.ActionLink("Tambah Pengguna", "Create", null, new { @class = "btn btn-outline-primary" })

                    <hr>
         
                    <table class="table table-bordered" id="example1" width="100%" cellspacing="0">
                        <thead>
                            <tr>
                                <th style="width:13%" class="align-middle">
                                    @Html.DisplayNameFor(model => model.Name)
                                </th>
                                <th style="width:8%" class="align-middle">
                                    @Html.DisplayNameFor(model => model.IC)
                                </th>

                                <th style="width:6%" class="align-middle">
                                    @Html.DisplayNameFor(model => model.Role)
                                </th>

                                <th style="width:8%" class="align-middle">
                                    @Html.DisplayNameFor(model => model.Email)
                                </th>
                                <th style="width:8%" class="align-middle">
                                    @Html.DisplayNameFor(model => model.Contact)
                                </th>
                                <th style="width:5%" class="align-middle">
                                    @Html.DisplayNameFor(model => model.BatchID)
                                </th>
                                <th style="width:5%" class="align-middle">
                                    @Html.DisplayNameFor(model => model.tb_status.Description)
                                </th>
                                <th style="width:26%" class="align-middle">Kemaskini</th>
                        </thead>

                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.Name)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.IC)
                                    </td>

                                    <td>
                                        @if (item.Role == 1)
                                        {
                                            <p>Admin</p>
                                        }
                                        else
                                        {
                                            <p>Tutor</p>
                                        }
                                    </td>

                                    <td>
                                        @Html.DisplayFor(modelItem => item.Email)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.Contact)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.BatchID)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.tb_status.Description)
                                    </td>
                                    <td>


                                        @Html.ActionLink("Lihat Butiran", "Details", null, new { id = item.ID }, new { @class = "btn btn-info my-1" })


                                        @Html.ActionLink("Kemaskini", "Edit", new { id = item.ID }, new { @class = "btn btn-warning my-1" })

                                        @Html.ActionLink("Padam", "Delete", new { id = item.ID }, new { @class = "btn btn-danger my-1" })



                                    </td>

                                </tr>
                            }

                        </tbody>

                    </table>





                </div>
            </div>
        </div>
    </div>
</body>



